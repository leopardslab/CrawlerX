apiVersion: v1
kind: Service
metadata:
  name: {{ include "crawlerx_helm.fullname" . }}
  labels:
    {{- include "crawlerx_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crawlerx_helm.selectorLabels" . | nindent 4 }}
